name: loom_core
description: The content spine — Work/Segment/Layer/Alignment/Position, the cursor law, ephemera decay. Pure Dart.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.5.0
dev_dependencies:
  test: ^1.25.0
