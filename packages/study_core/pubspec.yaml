name: study_core
description: The Trellis study engine, verbatim — SM-2 with the monotonic floor, prerequisite DAG, four typed recall items, strict .ohcourse parser. Pure Dart.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.5.0
dev_dependencies:
  test: ^1.25.0
